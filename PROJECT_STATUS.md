# 말씀소리 작업 현황

실행: `python preview.py` 후 http://localhost:5173 (의존성 설치 불필요).
Vite 실행: `npm install`, `npm run dev`. 배포 빌드: `npm run build`.
검사: `node --test tests/reader.test.cjs`.

실제 실행 진입점은 `index.html`이며 `public/reader.js`, `public/reader.css`, `public/bible-data.js`를 사용합니다. `src/` React 파일은 사용되지 않는 예전 시제품입니다. 변경 전 HTML은 `archive/index-before-dictation.html`에 보관했습니다.

## 변경

- 원문 정답 비교와 타이머만으로 읽음 처리하는 기능을 제거했습니다.
- 각 절 아래에 인식된 문장을 그대로 표시하고 보관합니다. 중간 결과는 흐리게 표시하며 확정된 기록만 집계합니다.
- 받아쓰기가 있고 설정한 무음 간격이 지나면 다음 절로 이동합니다. 1 / 1.8 / 2.8 / 4초 (새 사용자의 기본값은 1초) 중 선택하며, 버튼으로 직접 완료할 수도 있습니다.
- 같은 절 중복 집계, 중지/장 이동 후 늦게 온 이벤트 반영을 방지합니다.
- 날짜별 완료 기록으로 목표, 읽은 책/장/절과 PNG 공유 카드를 생성합니다.
- 새 저장 키는 `malssum_dictation_v3`입니다. 기존 타이머 기록을 실제 받아쓰기 실적에 합치지 않으며 이전 저장 키는 삭제하지 않습니다.
- 모바일과 데스크톱 레이아웃을 차분한 녹색과 종이색으로 정리했습니다.

## 남은 제약

- 사용자 제공 개역개정 4판 파일을 연결했습니다. 66권·1,189장, 절 번호 기준 31,102절(없음 표기 포함)입니다.
- 절 경계는 침묵으로 추정하므로 문장 중간에 오래 쉬면 일찍 넘어갈 수 있습니다. 이때 쉬는 간격을 길게 설정합니다.
- 브라우저 SpeechRecognition의 지원 여부, 권한, 네트워크에 영향을 받습니다. 배포에는 HTTPS가 필요하고 실제 휴대폰 마이크 검증이 남아 있습니다.
- 계획은 하루 절 수 목표이며 날짜별 성경 범위 배정은 아직 없습니다.
- 기기 간 동기화, 서버 저장, 통독 회차별 재집계는 지원하지 않습니다. 브라우저 데이터를 지우면 기록이 사라집니다.

## 검증

자동 검사는 침묵 미집계, 틀린 문장 보존과 자동 이동, 중간/중복 결과, 중지 및 장 이동 후 이벤트 차단, 새로고침 복원, 미수록 본문, 권한 오류를 포함합니다. 실제 음성을 흉내 낸 인식 이벤트 검사이며 실제 마이크 정확도 검증은 아닙니다.
브라우저에서 모바일 화면, 공유 카드와 PNG 생성 완료 표시를 확인했습니다.

공유 개선: 누적 완료 절 수 / 개역개정 31,073절 기준 전체 진도율을 카드, PNG, 공유 문구에 표시합니다. 기준 출처: https://www.meesung.co.kr/new/pdf/genesis16_book.pdf . 휴대폰 기본 공유창에서 카카오톡을 선택하는 방식이며, 미지원 환경은 이미지 저장/문구 복사를 제공합니다. 실제 카카오톡 발송은 수행하지 않았습니다. 자동 검사 9개와 빌드 통과.


기기 이전/복구: 사용자 선택에 따라 로그인 없는 파일 백업 방식을 적용했습니다. 전체 받아쓰기, 완료 날짜, 읽던 위치, 계획/설정을 JSON 파일로 내보내고 다른 기기에서 미리보기 후 병합 복원합니다. 중복 절은 한 번만 집계하고 현재 완료 기록을 우선합니다. 형식/버전/날짜/크기 검증과 저장 실패 시 기존 상태 보존을 적용했습니다. 자동 클라우드 동기화는 없으며 분실 대비를 위해 사용자가 주기적으로 다른 기기나 클라우드에 파일을 보관해야 합니다. 자동 검사 13개와 빌드 통과.


코스 일정 개선: 30일 코스(하루 1,036절), 시작일 편집, 경과일/남은 날/종료 예정일 표시를 추가했습니다. 기간은 30/365/730/1095일이며 전체 31,073절에 맞춰 기존 일일 목표를 86/43/29절로 올림 보정했습니다. 시작 당일은 경과 0일, 남은 날에 오늘을 포함합니다. 목표를 넘기는 첫 절 완료 시 낭독을 멈추고 완료 배너를 표시하며 다음 절 위치를 저장합니다. 장 끝에서는 다음 장 위치를 저장하되 미수록 본문은 건너뛰지 않습니다. 시작일은 백업에도 포함하고 구버전 백업은 오늘로 보완합니다. 자동 검사 21개 및 빌드 통과.



## 제공 성경 파일 적용
- CP949 파일을 UTF-8 데이터로 변환했습니다. 재생성: `python scripts/import_bible.py <원본파일경로>`.
- 45개 이어지는 문장, 11개 합쳐진 절 범위, 시편 권 구분과 붙어 있던 책 경계를 처리했습니다. 합쳐진 절은 한 카드로 표시하고 범위 내 미완료 절만 완료 처리합니다.
- 사도행전 24:7은 원본 24:6의 ‘6하반–8상반 없음’ 표기를 근거로 없음 안내를 넣어 이후 절 번호를 유지했습니다. 절 번호 기준 총합은 31,102이며 기존 31,073 상수를 이에 맞췄습니다.
- 기존 저장 키와 책 ID를 유지했습니다. 본문 전체, 이어진 문장, 합쳐진 절 완료 및 중복 방지 검사를 추가했습니다.


## 휴대폰 음성 인식 안정화
- 같은 장의 다음 절로 이동할 때 인식기를 종료하지 않고, 이미 처리한 결과 인덱스를 분리합니다.
- 변경된 인식 결과만 갱신하며 한 발화에서 누적된 동일 접두 문구와 자동 재연결 직후의 재전송을 병합합니다. 원문 정답으로 받아쓰기를 교체하지 않습니다.
- 빈 연결 종료가 반복되면 최대 두 번 재시도 후 안내하고 중지합니다. 페이지 진입에서 권한을 허용했다면 시작 버튼에서 마이크 확인용 스트림을 다시 열지 않습니다.
- 검사 32개와 빌드 통과. 실제 Android 휴대폰 마이크 정확도는 사용자 재확인이 필요합니다.


## 휴대폰 조기 절 넘김 방지
- 발화/소리 감지 중에는 부분 확정 결과가 와도 완료 타이머를 실행하지 않습니다.
- 인식 서비스 종료 시 완료 타이머를 취소하며, 종료와 재접속을 읽기 완료로 간주하지 않습니다.
- 휴대폰의 자동 넘김 간격은 최소 2.8초로 표시·적용하고 PC의 기존 간격은 유지합니다.
- 중간 발화, 소리 재개, 연결 종료 사례를 포함한 검사 35개 통과. 실제 휴대폰 낭독은 추가 확인이 필요합니다.


## 재연결 후 이전 절 재전송 차단
- 10절 완료 → 11절 이동 → 인식기 종료·재연결 → 10절 재전송 → 12절 조기 이동 사례를 실패 검사로 재현한 뒤 수정했습니다.
- 완료된 절의 음성 텍스트를 다음 절 경계에 보관해 인덱스가 초기화되어도 재전송을 제외합니다. 띄어쓰기·문장부호 차이와 이전 절+새 절이 붙은 결과를 처리합니다.
- 휴대폰은 절 이동·자동 재연결 뒤 새 발화 시작을 기다립니다. 새 확정 결과 없이 기존 초안이나 음성 종료 이벤트만으로 절을 완료하지 않습니다.
- 사용자가 직접 낭독 시작을 누르면 이전 절 재전송 필터를 초기화하므로 같은 문장을 의도적으로 다시 읽을 수 있습니다.
- 40개 검사 및 빌드 통과. 이미 저장된 기록은 자동 삭제하지 않습니다. 실제 휴대폰에서의 검증은 별도입니다.


## 인식 종료 뒤 절 이동 정지 수정
- 휴대폰의 final → speechend → end 순서에서 기존 타이머가 취소되어 1절에 머무는 현상을 재현했습니다.
- 새 확정 텍스트와 말하기 종료가 확인되어 완료 대기 중이었다면, 서비스가 닫혀도 대기를 유지합니다. 다음 절 이동 후 인식기를 한 번 연결합니다. 말하기 종료 없는 연결 오류나 미확정 문장이 남은 경우에는 완료하지 않습니다.
- speechstart 이벤트가 누락되는 브라우저에서도 기존 절 재전송 필터를 통과한 새 확정 결과를 받아들입니다.
- 중지·장 이동은 대기 중인 완료를 취소하며, 사용자가 완료 버튼을 먼저 누르면 한 번만 이동·재연결합니다.
- 44개 검사와 빌드 통과. 실제 휴대폰 음성 환경은 별도 재확인이 필요합니다.

## 휴대폰 쉬는 시간 선택 복원
- 휴대폰에서도 1초, 1.8초, 2.8초, 4초를 모두 선택할 수 있습니다. 선택값을 그대로 적용하며 말소리가 재개되면 완료 타이머를 취소합니다.
- 적용 버전 표시와 업데이트 확인 기능을 추가했고 정적 자산 URL에 버전을 붙여 이전 JavaScript 캐시가 남지 않도록 했습니다.
- 버전 `2026.09.25.3`, 검사 45개와 빌드 통과.
